{
    "homepage": "http://lucasccesar.github.io/nerve",
    "dependencies": {
        "jwt-decode": "^4.0.0"
    },
    "scripts": {
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build"
    },
    "devDependencies": {
        "gh-pages": "^6.1.0"
    }
}
